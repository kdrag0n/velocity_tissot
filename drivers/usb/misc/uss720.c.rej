--- drivers/usb/misc/uss720.c
+++ drivers/usb/misc/uss720.c
@@ -715,11 +715,6 @@ static int uss720_probe(struct usb_interface *intf,
 
 	interface = intf->cur_altsetting;
 
-	if (interface->desc.bNumEndpoints < 3) {
-		usb_put_dev(usbdev);
-		return -ENODEV;
-	}
-
 	/*
 	 * Allocate parport interface 
 	 */
