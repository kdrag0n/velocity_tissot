--- drivers/usb/host/ehci-orion.c
+++ drivers/usb/host/ehci-orion.c
@@ -226,8 +226,7 @@ static int ehci_orion_drv_probe(struct platform_device *pdev)
 	priv->phy = devm_phy_optional_get(&pdev->dev, "usb");
 	if (IS_ERR(priv->phy)) {
 		err = PTR_ERR(priv->phy);
-		if (err != -ENOSYS)
-			goto err_phy_get;
+		goto err_phy_get;
 	} else {
 		err = phy_init(priv->phy);
 		if (err)
