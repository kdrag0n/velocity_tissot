--- drivers/scsi/sd.c
+++ drivers/scsi/sd.c
@@ -3235,9 +3232,6 @@ static int sd_resume(struct device *dev)
 	struct scsi_disk *sdkp = scsi_disk_get_from_dev(dev);
 	int ret = 0;
 
-	if (!sdkp)	/* E.g.: runtime resume at the start of sd_probe() */
-		return 0;
-
 	if (!sdkp->device->manage_start_stop)
 		goto done;
 
