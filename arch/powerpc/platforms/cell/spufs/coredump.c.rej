--- arch/powerpc/platforms/cell/spufs/coredump.c
+++ arch/powerpc/platforms/cell/spufs/coredump.c
@@ -174,8 +174,6 @@ static int spufs_arch_write_note(struct spu_context *ctx, int i,
 	if (!dump_skip(cprm,
 		       roundup(cprm->written - total + sz, 4) - cprm->written))
 		goto Eio;
-
-	rc = 0;
 out:
 	free_page((unsigned long)buf);
 	return rc;
