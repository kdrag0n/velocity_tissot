--- block/bio-integrity.c
+++ block/bio-integrity.c
@@ -165,9 +165,6 @@ bool bio_integrity_enabled(struct bio *bio)
 	if (!bio_is_rw(bio))
 		return false;
 
-	if (!bio_sectors(bio))
-		return false;
-
 	/* Already protected? */
 	if (bio_integrity(bio))
 		return false;
